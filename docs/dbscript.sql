-- created popballoon.sqlite file from the command line and placed in the home directory
-- create a new table
CREATE TABLE scores(key INTEGER PRIMARY KEY, name VARCHAR(100), level INTEGER, score INTEGER);
--insert into scores(key, name, level, score) values(1, 'JRubens', 1, 100);